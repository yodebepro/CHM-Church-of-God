/* chm-public-renderer.js — DISABLED
   Rendering is now handled entirely by cms-public.js which correctly
   places content inside each page's designated section containers.
   This file is kept as a placeholder so existing <script> tags do not 404. */
