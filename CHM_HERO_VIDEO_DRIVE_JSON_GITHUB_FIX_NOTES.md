# CHM Hero Background Video + Google Drive + JSON/GitHub Update

- Preserves the current Leadership Exact Cards baseline and every existing filename.
- Homepage hero video now renders behind the “Where Faith Meets Transformation” section.
- Admin supports video upload and pasted links.
- Supported links: direct MP4/WebM, public Google Drive `/videos/d/.../play`, Google Drive file/share links, YouTube, and Vimeo.
- Google Drive files must be shared as “Anyone with the link.” The renderer tries a direct stream first and falls back to the Google Drive preview player.
- CHM True CMS Global Publisher now includes explicit JSON export, JSON import, and Push All to GitHub controls.
- About Page Editor → Page Hero now includes hero upload/link/preview/save plus Page Hero JSON import/export and Push to GitHub.