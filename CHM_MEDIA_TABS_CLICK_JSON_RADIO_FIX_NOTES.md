# Media Tabs Click, JSON, and Radio #1 Fix

Only Media Control was changed.

- Listen Live, Watch Live, and General Media tabs now use reliable event listeners and switch to their proper settings.
- The JavaScript syntax error that prevented all tab clicks was removed.
- JSON Export Current Tab, Export All Media, and Import JSON are now visible above the settings area and remain available in the form.
- The existing CHM radio stream `https://c13.radioboss.fm:8254/stream` is preserved as protected Listen Live item #1 and the default source.
- Everything else remains unchanged.
